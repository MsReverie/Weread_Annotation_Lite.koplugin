# Weread Local Annotations

独立的 KOReader 插件：为本地 EPUB/TXT 等可重排文档显示微信读书划线，并按需加载和预取想法。

当前开发目标：

- 不下载或打开微信读书在线书籍；
- 不修改本地 EPUB；
- 划线和想法使用插件自己的 SQLite 数据库；
- 点击未缓存划线时单条加载想法；
- 当前章节按 5 条一批预取想法，并可继续预取下一章节；
- 支持取消、断点续传、失败重试和离线查看缓存。

第一版只支持 KOReader CREngine 可重排文档，不支持 PDF 和扫描文档。
